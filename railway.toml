[build]
builder = "NIXPACKS"
buildCommand = "npm install --omit=dev && npm run build"

[deploy]
startCommand = "npm start"
releaseCommand = "if [ -n \"$DATABASE_URL\" ]; then cd server && npx prisma migrate deploy; fi"
